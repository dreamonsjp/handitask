<?php
App::uses('AppController', 'Controller');
/**
 * Personalities Controller
 *
 * @property Personality $Personality
 * @property PaginatorComponent $Paginator
 */
class PersonalitiesController extends AppController {

/**
 *  Layout
 *
 * @var string
 */
	public $layout = 'bootstrap';

/**
 * Helpers
 *
 * @var array
 */
	public $helpers = array('TwitterBootstrap.BootstrapHtml', 'TwitterBootstrap.BootstrapForm', 'TwitterBootstrap.BootstrapPaginator');
/**
 * Components
 *
 * @var array
 */
	public $components = array('Paginator');
/**
 * index method
 *
 * @return void
 */
	public function index() {
		$this->Personality->recursive = 0;
		$this->set('personalities', $this->Paginator->paginate());
	}

/**
 * view method
 *
 * @param string $id
 * @return void
 */
	public function view($id = null) {
		$this->Personality->id = $id;
		if (!$this->Personality->exists()) {
			throw new NotFoundException(__('Invalid %s', __('personality')));
		}
		$this->set('personality', $this->Personality->read(null, $id));
	}

/**
 * add method
 *
 * @return void
 */
	public function add() {
		if ($this->request->is('post')) {
			$this->Personality->create();
			if ($this->Personality->save($this->request->data)) {
				$this->Session->setFlash(
					__('The %s has been saved', __('personality')),
					'alert',
					array(
						'plugin' => 'TwitterBootstrap',
						'class' => 'alert-success'
					)
				);
				$this->redirect(array('action' => 'index'));
			} else {
				$this->Session->setFlash(
					__('The %s could not be saved. Please, try again.', __('personality')),
					'alert',
					array(
						'plugin' => 'TwitterBootstrap',
						'class' => 'alert-error'
					)
				);
			}
		}
	}

/**
 * edit method
 *
 * @param string $id
 * @return void
 */
	public function edit($id = null) {
		$this->Personality->id = $id;
		if (!$this->Personality->exists()) {
			throw new NotFoundException(__('Invalid %s', __('personality')));
		}
		if ($this->request->is('post') || $this->request->is('put')) {
			if ($this->Personality->save($this->request->data)) {
				$this->Session->setFlash(
					__('The %s has been saved', __('personality')),
					'alert',
					array(
						'plugin' => 'TwitterBootstrap',
						'class' => 'alert-success'
					)
				);
				$this->redirect(array('action' => 'index'));
			} else {
				$this->Session->setFlash(
					__('The %s could not be saved. Please, try again.', __('personality')),
					'alert',
					array(
						'plugin' => 'TwitterBootstrap',
						'class' => 'alert-error'
					)
				);
			}
		} else {
			$this->request->data = $this->Personality->read(null, $id);
		}
	}

/**
 * delete method
 *
 * @param string $id
 * @return void
 */
	public function delete($id = null) {
		if (!$this->request->is('post')) {
			throw new MethodNotAllowedException();
		}
		$this->Personality->id = $id;
		if (!$this->Personality->exists()) {
			throw new NotFoundException(__('Invalid %s', __('personality')));
		}
		if ($this->Personality->delete()) {
			$this->Session->setFlash(
				__('The %s deleted', __('personality')),
				'alert',
				array(
					'plugin' => 'TwitterBootstrap',
					'class' => 'alert-success'
				)
			);
			$this->redirect(array('action' => 'index'));
		}
		$this->Session->setFlash(
			__('The %s was not deleted', __('personality')),
			'alert',
			array(
				'plugin' => 'TwitterBootstrap',
				'class' => 'alert-error'
			)
		);
		$this->redirect(array('action' => 'index'));
	}

}
